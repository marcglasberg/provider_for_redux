name: provider_for_redux
description: Let's you use Provider with Redux. Uses AsyncRedux, the non-boilerplate version of Redux (https://pub.dev/packages/async_redux).
version: 5.0.0
# author: Marcelo Glasberg <marcglasberg@gmail.com>
homepage: https://github.com/marcglasberg/provider_for_redux

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  async_redux: ^15.0.0
  flutter:
    sdk: flutter
  path_provider: ^2.0.5
  provider: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
