name: example
description: Example for provider_for_redux.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  async_redux: ^21.0.1
  flutter:
    sdk: flutter
  http: ^0.13.5
  path_provider: ^2.1.1
  provider: ^6.1.1
  provider_for_redux:
    path: ../
  tuple: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
