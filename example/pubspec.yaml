name: example
description: Example for provider_for_redux.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: '>=2.13.0 <3.0.0'

dependencies:
  async_redux: ^13.0.4
  flutter:
    sdk: flutter
  http: ^0.13.3
  path_provider: ^2.0.5
  provider: ^6.0.0
  provider_for_redux:
    path: ../
  tuple: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true
